using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using Team29_Group_Project;

namespace Team29TestProject
{
    [TestClass]
    public class PatientViewTest
    {
        /*
        [TestMethod]
        public void TestIfMessageToShow()
        {
            PatientFactory patientFactory = new PatientFactory();
            Patient p = patientFactory.GetPatientType(PatientTypes.Free);

            int patientID = 1;

            p.PatientID = patientID;
            p.firstName = "Mark";
            p.isFree = true;

            AppointmentFactory factory = new AppointmentFactory();
            Appointment a = factory.GetAppointmentBand(AppointmentBand.Band1);

            a.appointmentID = 1;
            a.patientID = patientID;
            a.appointmentDate = DateTime.Today.Date.AddDays(-1);
            a.arrivedToAppointment = "No";

            PatientDetailsModel pdm = new PatientDetailsModel();



            bool messagesToView = pdm.messagesToView(patientID);

            Assert.IsTrue(messagesToView);


        }
        */
    }
}
